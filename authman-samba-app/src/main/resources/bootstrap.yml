spring:
  application:
    name: smb-con
  cloud:
    config:
      enabled: ${CONFIG_CLIENT_ENABLED:false}
      uri: ${CONFIG_URI:https://github.com/bremersee/config}
      username: ${CONFIG_USER:user}
      password: ${CONFIG_PASSWORD:changeit}
